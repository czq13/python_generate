cmake_minimum_required(VERSION 3.0.0)
project(Auto_Generate_For_28377 VERSION 0.1.0)

#set(execute_mav 1)
#set(execute_test_env 1)
set(execute_ulog_demo 1)
add_definitions(-std=c++11)

if (execute_mav)
include(mav_pyutils/mav_pyutils.cmake)
elseif (execute_msg)
elseif (execute_param)
elseif (execute_test_env)
include(test_env/test_env.cmake)
elseif (execute_ulog_demo)
include (msg_pyutils/ulog_demo/ulog_demo.cmake)
endif(execute_mav)
enable_testing()

add_compile_options(-fgnu89-inline)
set(CPACK_PROJECT_NAME ${PROJECT_NAME})
set(CPACK_PROJECT_VERSION ${PROJECT_VERSION})
include(CPack)
